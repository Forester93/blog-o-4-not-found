            <li class="list-group-item d-flex justify-content-between align-items-start" comment_id={{cid}}>
            <div class="ms-2 me-auto">
            <strong class="fw-bold">{{username}}</strong> commented:<br>
            {{comment}}
            </div>
            <span class="badge text-muted">On {{date}}</span>
            {{#if logged_in}}
            <button class="deleteComment badge bg-black px-1 rounded-pill">❌</button>
            {{/if}}
            </li>